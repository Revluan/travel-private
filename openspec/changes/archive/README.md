# Archived changes

Completed OpenSpec changes live here, named `<YYYY-MM-DD>-<change-name>/`.

Each archive folder is a frozen historical record — read it to understand what was changed and why, but don't edit. The live, source-of-truth specs live in `openspec/specs/` and were merged from these archives.

## Recent archives

(none yet)
